import { Flex } from "@chakra-ui/react";
import React from "react";

export default function SubSectionUnknownChanges() {
	return (
		<Flex ms={9}>
			You don't have any files to update!
		</Flex>
	);
}